# Requirements for running the Python examples in this repo
# Install with: pip install -r requirements.txt

# Async HTTP — used by fetch-api examples and skill
httpx>=0.27.0

# Data processing — used by migrate-data and visualize examples
pandas>=2.1.0
pyarrow>=15.0.0

# Visualisation — used by visualize examples
matplotlib>=3.8.0
seaborn>=0.13.0

# Optional: faster Parquet with snappy compression
# (included in pyarrow, listed here for clarity)
# pyarrow already includes snappy support
